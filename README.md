# Simple test for biiif

Usage:

```bash
$ npm install
$ bash prepare.sh
$ node index.js
```

Then open http://localhost:8080 in your browser. Select a manifest via the button in the upper left corner.
